# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx/
**/build/

# Keep the Gradle wrapper jar committed (do NOT ignore it):
!gradle/wrapper/gradle-wrapper.jar

# Native artifacts are built in CI, not committed:
app/libs/*.aar
app/src/main/jniLibs/**/*.so

# Keystores / secrets
*.jks
*.keystore
keystore.properties
